# Smart-Record-Deduplication-Engine
A smart record management system that cleans and optimizes sorted data streams by removing redundant entries while preserving meaningful information. This project demonstrates efficient linked list manipulation, pointer-based traversal, duplicate detection
